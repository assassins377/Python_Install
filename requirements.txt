# Runtime-зависимости проекта (для совместимости с `pip install -r`).
# Канонический источник — pyproject.toml ([project.dependencies]).
# Dev-инструменты (pytest, pytest-cov, ruff) — в extras: `pip install -e .[dev]`.
wxPython>=4.2.0
psutil>=5.9.0
